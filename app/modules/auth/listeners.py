"""Auth-owned event listeners.

Auth reacts to nothing itself for now. Cross-module reactions live in the
consuming module (subscribe via `@on` in its `listeners.py`). Kept as the
conventional home for any future auth listeners.
"""

from __future__ import annotations
